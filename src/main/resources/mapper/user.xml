<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- DAO 정보 -->
<mapper namespace="com.project.pincafe.user.UserDAO">

<!-- resultMap = 얻어온 결과를 저장할 곳. 즉 VO 정보 -->
<resultMap type="com.project.pincafe.user.UserTblVO" id="userMap">
    <result property="id"        column="id"/>
    <result property="nickname"  column="nickname"/>
    <result property="password"  column="password"/>
    <result property="email"     column="email"/>
    <result property="address"   column="address"/>
    <result property="fileCode"  column="file_code"/>

</resultMap>

<select id="selectOneUserForLogin" resultMap="userMap">
    SELECT id, password, nickname FROM USER_TBL
    WHERE id=#{id}
    AND password=#{password}
</select>

<select id="selectOneUserForId" resultMap="userMap">
    SELECT id FROM USER_TBL
    WHERE id=#{id}
</select>

<select id="selectOneUserForNick" resultMap="userMap">
    SELECT nickname FROM USER_TBL
    WHERE nickname=#{nickname}
</select>

<insert id="insertUser">
    INSERT INTO USER_TBL (id, nickname, password, email, address, file_code)
    VALUES (#{id}, #{nickname}, #{password}, #{email}, #{address}, #{fileCode})
</insert>

<select id="getIdByNnEmail" resultMap="userMap">
    SELECT id, nickname, password, email FROM USER_TBL WHERE email = #{email} AND nickname = #{nickname}
</select>

<select id="getPwByIdEmail" resultMap="userMap">
    SELECT id, nickname, password, email FROM USER_TBL WHERE id = #{id} AND email = #{email}   
</select>

</mapper>